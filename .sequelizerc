const path = require('path');

module.exports = {
  'config': 'sequelizeMigrationsConfig.js',
  'migrations-path': path.resolve('migrations', 'sequelize'),
  'seeders-path': path.resolve('migrations', 'sequelize', 'seeds'),
}
